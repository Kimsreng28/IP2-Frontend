"use client";
import { useTheme } from "next-themes";
export default function Users() {
  return (

    <div 
    // className={`rounded-lg p-6 shadow-md flex flex-col gap-4
    //   ${theme === "dark" ? "bg-gray-800 text-white" : "bg-white text-gray-800"}`}
      >
        <h1>User Page </h1>
      </div>
  );
}
